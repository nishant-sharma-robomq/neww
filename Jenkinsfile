pipeline {
    agent any
    
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    // def imageName = 'my-custom-image'
                    sh 'ls'
                    // def dockerfilePath = '/var/jenkins_home/workspace/dummy'
                    
                    // Build the Docker image using Docker CLI
                    // sh "docker build -t ${imageName} -f ${dockerfilePath} ."
                }
            }
        }
    }
}
